1) ho creato results ma non è bene implementato. Aggiungerci la scrittura dei dati dei vettori di eulero. FATTO
2) è necessasio salvare le velocità? posso evitare di mantenenrle in memoria o rimuoverle dopo averle usate per generare i vettori? o sono necessarie per il lavoro successivo? NECESSARIO PER IL FUNZIONAMENTO DI FNC_SV2EV_C
3) per come ho implementato il ciclo, ogni iterazione varia noise e stdDev sul numero di stazioni generato per ogni città, che essendo definito fuori dal ciclo rimane fisso per ogni elemento (ad esempio, variazione di noise e stdDev su 5,12,15 stazioni). Ciò è corretto o porta a dati "stagnanti"? Dovrei generarlo randomicamente per ogni città con un sottociclo? è persino possibile moidificare un indice del ciclo dentro al ciclo? dovrei mettere poi un comando per evitare che i numeri casuali debbano essere univoci, ad esempio evitare 5,12,12 stazioni? 
4) dimensioni del parametro numStazrange in cicli successivi: nella cella risultati viene creato un asse di lunghezza n, dove ad ogni iterazione del ciclo ogni n assume valore casuale. Eseguendo il ciclo con lo stesso n, i valori precedenti vengono tutti sovrascritti? eseguendo il ciclo con un n minore, i valori di n superiori all'n attuale sono mantenuti dal ciclo precedente e sono sovrascritti solo i valori fino all'n del ciclo attuale?
5) la matrice contiene dati struct contenenti, per indice noiseIdx, stdDevIdx, stationIdx i segunti elementi: 'EV_AD',EV_AD_result, 'EV_AP',EV_AP_result, 'Noise',noiseI, 'StdDev',stdDevJ, 'Stations',stationK
devo nominare correttamente i vettori riferendoli a noiseI, stdDevJ, stationK e rimuovere la presenza di noiseI, stdDevJ, stationK come dati nella matrice. Infatti se volessi confrontarli potrei filtrarli solo in base al nome.